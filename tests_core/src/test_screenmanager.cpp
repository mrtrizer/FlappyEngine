#include "catch.h"
#include "fakeit.h"

#include <memory>

#include <core/managerlist.h>
#include <core/screenmanager.h>

using namespace flappy;
using namespace std;

TEST_CASE( "ScreenManager::resize() screenSize() width() height()") {
    REQUIRE(true);
}
